__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
generated_artifacts/
node_modules/
frontend/dist/
frontend/.vite/
.env
.env.local
.DS_Store
Thumbs.db
# Office temporary lock files
~$*

# Backend scratch / preview images (e.g. _t45.png, _fig*_preview.png)
backend/_*.png
.vscode/
.idea/

# Generated sample / verification report PDFs
# (VERIFY_* was tracked from before this rule existed and was re-committed on every
#  regeneration — gitignore never applies to an already-tracked file. Untracked at C274.)
PFC_Report_VERIFY_*.pdf
PFC_Report_REALDESIGN_*.pdf
# GUI-exported reports (session-id filenames). Two of these under specs/Review were 26 MB
# of the 33 MB that made specs/ look too big to track — build output, not source material.
PFC_Report_pfc-*.pdf

# Generated control-design chapter PDFs (regenerated from the report builders)
PFC_Chapter6_*.pdf

# Controller reference DB — derived BM25 index cache (rebuilt from the PDFs/manifest)
backend/data/controllers/.index.json
backend/app/mode_b/semiconductor/parts/
