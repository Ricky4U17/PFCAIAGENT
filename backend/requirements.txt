fastapi~=0.115.0
uvicorn~=0.30.0
pydantic~=2.8.0
numpy~=2.0.0
scipy~=1.14.0
matplotlib~=3.9.0
reportlab~=4.2.0
schemdraw~=0.23
pypdf~=4.3.0
pillow~=10.4.0
langgraph~=0.2.34
pytest~=8.3.0
# Pinned <0.28: httpx 0.28 removed the TestClient(app=...) shortcut that starlette 0.27
# relies on, which breaks FastAPI TestClient in the test suite (TypeError on 'app' kwarg).
# httpx is a test-only dependency here (the app itself never imports it). Do not bump to 0.28+
# unless starlette is also upgraded to a version whose TestClient uses transport=ASGITransport.
httpx==0.27.2
anthropic>=0.34.0
pdfplumber>=0.11.0
python-multipart>=0.0.9
